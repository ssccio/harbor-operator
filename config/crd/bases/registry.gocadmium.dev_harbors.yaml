---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: harbors.registry.gocadmium.dev
spec:
  group: registry.gocadmium.dev
  names:
    kind: Harbor
    listKind: HarborList
    plural: harbors
    singular: harbor
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.hostname
      name: Hostname
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.readyComponents
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Harbor is the Schema for the harbors API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HarborSpec defines the desired state of Harbor.
            properties:
              adminPasswordSecretRef:
                description: |-
                  adminPasswordSecretRef names a Secret containing key password for the
                  Harbor admin account. Operator creates it with a generated password if absent.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              database:
                description: database configures the PostgreSQL backend. Uses host
                  Postgres at 10.0.0.100 by default.
                properties:
                  secretRef:
                    description: 'secretRef names a Secret containing keys: host,
                      port, username, password, database.'
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - secretRef
                type: object
              hostname:
                description: |-
                  hostname is the public FQDN for Harbor (e.g. harbor.sscc.io).
                  Used for the Traefik IngressRoute and Harbor's externalURL.
                minLength: 1
                type: string
              image:
                default: v2.13.0
                description: |-
                  image is the Harbor version tag applied to all component images
                  (e.g. "v2.13.0"). All goharbor/* images share a version tag.
                type: string
              redis:
                description: redis configures the Redis instance for caching and job
                  queuing.
                properties:
                  secretRef:
                    description: |-
                      secretRef names a Secret containing key REDIS_URL (redis://[:password@]host:port/db).
                      Required when type is external.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  type:
                    default: internal
                    description: type selects internal (operator-managed Deployment)
                      or external Redis.
                    enum:
                    - internal
                    - external
                    type: string
                type: object
              resources:
                description: resources sets CPU/memory requests and limits applied
                  to all Harbor components.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              storage:
                description: storage configures artifact storage for the registry
                  component.
                properties:
                  retain:
                    default: true
                    description: retain prevents PVC deletion when the Harbor CR is
                      deleted. Defaults to true.
                    type: boolean
                  s3:
                    description: s3 configures S3-compatible storage. Required when
                      type is s3.
                    properties:
                      bucket:
                        description: bucket is the S3 bucket name for Harbor registry
                          blobs.
                        minLength: 1
                        type: string
                      endpoint:
                        description: endpoint is the S3 endpoint URL (e.g. http://minio.minio.svc:9000).
                        minLength: 1
                        type: string
                      region:
                        default: us-east-1
                        description: region is the S3 region. Defaults to us-east-1
                          for MinIO.
                        type: string
                      secretRef:
                        description: 'secretRef names a Secret containing keys: accessKey,
                          secretKey.'
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - bucket
                    - endpoint
                    - secretRef
                    type: object
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 50Gi
                    description: size is the PVC size when type is filesystem. Defaults
                      to 50Gi.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    description: storageClassName overrides the default StorageClass
                      (filesystem mode only).
                    type: string
                  type:
                    default: s3
                    description: type selects the storage backend. Defaults to s3.
                    enum:
                    - s3
                    - filesystem
                    type: string
                type: object
              tls:
                description: tls controls Traefik IngressRoute TLS configuration.
                properties:
                  certResolver:
                    description: |-
                      certResolver names a Traefik certResolver for automatic TLS.
                      Leave empty to rely on Cloudflare edge TLS termination.
                    type: string
                  enabled:
                    default: true
                    description: enabled creates IngressRoutes on the websecure entrypoint.
                      Defaults to true.
                    type: boolean
                  entryPoint:
                    default: websecure
                    description: entryPoint is the Traefik entrypoint to bind. Defaults
                      to websecure.
                    type: string
                type: object
              trivy:
                description: trivy configures the vulnerability scanner.
                properties:
                  enabled:
                    default: true
                    description: enabled deploys the Trivy scanner sidecar. Defaults
                      to true.
                    type: boolean
                type: object
            required:
            - database
            - hostname
            type: object
          status:
            description: HarborStatus defines the observed state of Harbor.
            properties:
              conditions:
                description: |-
                  conditions reflect the current health of the Harbor resource.
                  Types: Available, Progressing, Degraded.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the .metadata.generation the controller
                  last acted on.
                format: int64
                type: integer
              phase:
                description: phase is a human-readable summary of the current lifecycle
                  state.
                type: string
              readyComponents:
                description: readyComponents is the count of Harbor components currently
                  Ready.
                format: int32
                type: integer
              totalComponents:
                description: totalComponents is the total count of managed Harbor
                  components.
                format: int32
                type: integer
              url:
                description: url is the public URL of the Harbor instance.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
